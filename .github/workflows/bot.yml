name: BEL VIP Bot Scheduler

on:
  schedule:
    # 8:30AM WAT (7:30AM UTC)
    - cron: '30 7 * * *'
    # 2:30PM WAT (1:30PM UTC)
    - cron: '30 13 * * *'
    # 7:30PM WAT (6:30PM UTC)
    - cron: '30 18 * * *'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run BEL VIP Bot
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          USE_POCKET_OPTION: ${{ secrets.USE_POCKET_OPTION }}
          PO_SESSION_TOKEN: ${{ secrets.PO_SESSION_TOKEN }}
          PO_USER_ID: ${{ secrets.PO_USER_ID }}
        run: |
          python bot.py
